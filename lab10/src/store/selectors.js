/* export const selectAllScores = (state) => state.scores.scores;
export const selectUserScores = (state, email) => 
  state.scores.scores.filter(score => score.userEmail === email);
export const selectGameScores = (state, gameName) => 
  state.scores.scores.filter(score => score.game === gameName);
 */